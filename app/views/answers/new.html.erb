<div class="create-answer">
<%= form_for @answer, url: question_answers_path(@question.id) do |f| %>

  <p><%= f.label :content, "Give an Answer" %></p>
  <p><%= f.text_area :content %></p>

  <%= f.submit "Publish" %>
<% end %>
</div>

